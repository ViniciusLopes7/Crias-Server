# Logs
Logs/
*.log

# Python (discord-bot)
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
env/
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# Go (discord-agent)
discord-agent/build/
discord-agent/vendor/
discord-agent/internal/proto/*.pb.go
*.exe
*.test
*.out

# Env files com secrets (NUNCA commitar)
.env
.env.local
.env.production
*.local.env
agent.local.yaml

# Editores
.vscode/
.idea/
*.swp
*.swo
*.swn
.DS_Store

# OS
Thumbs.db

# Worklog de desenvolvimento local (mantemos /home/z/my-project/worklog.md fora do repo)
worklog.md

# Artefatos de build locais
out/
release-assets/
tmp/

# airootfs sincronizado pelo sync-airootfs.sh (artefato de build, NÃO commitar).
# Estes arquivos são cópias do repo geradas antes do mkarchiso. Para regerar:
#   bash archiso-profile/sync-airootfs.sh
archiso-profile/airootfs/opt/crias-server/

# Logs de QEMU gerados por tests/iso-qemu-boot.sh
tests/fixtures/qemu-boot-last.log
