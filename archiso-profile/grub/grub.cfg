# GRUB configuration for Crias-Server ISO

insmod part_gpt
insmod part_msdos
insmod fat
insmod iso9660
insmod ntfs
insmod ntfscomp
insmod exfat
insmod udf
insmod png

set default="0"
set timeout=5

if [ -f /boot/grub/fonts/unicode.pf2 ]; then
    loadfont /boot/grub/fonts/unicode.pf2
    terminal_output gfxterm
elif [ -f /grub/fonts/unicode.pf2 ]; then
    loadfont /grub/fonts/unicode.pf2
    terminal_output gfxterm
fi

if [ -f /boot/grub/boot-bg.png ]; then
    background_image /boot/grub/boot-bg.png
elif [ -f /grub/boot-bg.png ]; then
    background_image /grub/boot-bg.png
fi

set color_normal=white/black
set color_highlight=cyan/black

menuentry "Crias Server OS (Arch Linux)" --class arch --class linux {
    set gfxpayload=keep
    linux /%INSTALL_DIR%/boot/%ARCH%/vmlinuz-linux archisobasedir=%INSTALL_DIR% archisosearchuuid=%ARCHISO_UUID%
    initrd /%INSTALL_DIR%/boot/%ARCH%/initramfs-linux.img
}

menuentry "Crias Server OS (Copy to RAM)" --class arch --class linux {
    set gfxpayload=keep
    linux /%INSTALL_DIR%/boot/%ARCH%/vmlinuz-linux archisobasedir=%INSTALL_DIR% archisosearchuuid=%ARCHISO_UUID% copytoram=y
    initrd /%INSTALL_DIR%/boot/%ARCH%/initramfs-linux.img
}
